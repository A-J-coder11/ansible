- name: variables from promt
  hosts: local
  connection: local
  tasks:
  - name: printing user information
      ansible.builtin.debug:
        msg: "Hello  {{ COURSE }}, duration is {{ DURATION }}, trainer is {{ NAME }}"